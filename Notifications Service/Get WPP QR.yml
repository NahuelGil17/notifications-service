info:
  name: Get WPP QR
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:3000/auth/whatsapp/qr
  headers:
    - name: X-Api-Key
      value: "{{X-Api-Key}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
